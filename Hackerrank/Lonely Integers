#include <cmath>
#include <cstdio>
#include <vector>
#include <iostream>
#include <algorithm>
using namespace std;


int main() {
    int n;
    cin>>n;
    int arr[100]={0};
    for(int i=0;i<n;i++)
        {
          int t;
        cin>>t;
        arr[t]++;
        }
    for(int i=0;i<=100;i++) 
        {
        if(arr[i]==1)
            {
            cout<<i<<" ";
            }
    }
        
    return 0;
}
